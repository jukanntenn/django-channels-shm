# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
target/

# Performance testing and observability artifacts (P3 baselines not committed + O4 data not committed)
.benchmarks/
bench/results/
target/criterion/
/dev/shm/*_obs/
*.jsonl
*.jsonl.*
!bench/**/*.json
!docs/**/*.json

# ── Harness: test/coverage/cache artifacts ──
.coverage
.pytest_cache/
.hypothesis/
.ruff_cache/

# ── Harness: AI agent local overrides ──
.claude/settings.local.json

# ── Harness: native build artifact (rebuilt via `uvx maturin develop --skip-install`) ──
src/channels_shm/_native.abi3.so

# ── Harness: cloned reference repos (exploration only, not part of project) ──
.local/

# ── Harness: Rust coverage data (generated by cargo-llvm-cov) ──
*.lcov
crates/**/*.info
*.profraw

.zcode/plans/

# ── Harness: e2e docker build artifacts ──
tests/e2e/.docker/
